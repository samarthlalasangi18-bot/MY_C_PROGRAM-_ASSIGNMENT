#include <stdio.h>
int main()
{ 
  printf("The main()function in c is the starting point of execution of the pgm\n");
  printf("without main() complier will not execute the pgm\n");
  printf("it tells the comipler from from where the pgm executoin starts\n");
  return 0;
}
